System Message:
$system_prompt

User Message:
Database ID: $db_id
SQLite file: $db_path

Schema:
$schema_overview

Foreign Keys:
$foreign_keys

Question: $question

Response requirements:
- Return exactly one valid SQL statement that answers the question.
- Do not include explanations, commentary, markdown fences, or the words "Answer"/"Solution".
- Output must contain only the SQL command (e.g., start with SELECT/INSERT/UPDATE/DELETE).

Answer:
